<Window x:Class="Space_shooter.Windows.CustomDifficultySettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Space_shooter.Windows"
        mc:Ignorable="d"
       Title="CustomDifficultySettings" Height="350" Width="300" WindowStartupLocation="CenterScreen"  WindowStyle="None" ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="FontSize" Value="30" />
            <Setter Property="MaxHeight" Value="60" />
            <Setter Property="Width" Value="150" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="Wheat"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" 
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                               VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" 
                          Value="True">
                                <Setter Property="Foreground" 
                           Value="Yellow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Windows/Window_Background.png"/>
        </Grid.Background>
        <StackPanel x:Name="MyStackpanel" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" Margin="30,70,15,30">
            <StackPanel Orientation="Horizontal" >
                <Label Content="Asteroids speed" Foreground="Yellow" FontFamily="Impact"/>
                <TextBox Text="{Binding Asteroidspeed, UpdateSourceTrigger=Explicit}" PreviewTextInput="TextBox_PreviewTextInput" MaxLength="2" HorizontalAlignment="Center" VerticalAlignment="Center" Width="70" Opacity="0.65"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                <Label Content="Player firerate" Foreground="Yellow" FontFamily="Impact"/>
                <TextBox Text="{Binding Firerate, UpdateSourceTrigger=Explicit}" PreviewTextInput="TextBox_PreviewTextInput" HorizontalContentAlignment="Right" VerticalAlignment="Center" Width="70" Opacity="0.65"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" >
                <Label Content="Powerup dropping rate" Foreground="Yellow" FontFamily="Impact"/>
                <TextBox Text="{Binding Poweruprate, UpdateSourceTrigger=Explicit}" PreviewTextInput="TextBox_PreviewTextInput" MaxLength="2" HorizontalContentAlignment="Right" VerticalAlignment="Center" Width="70" Opacity="0.65"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" >
                <Label Content="Enemy shooting intensity" Foreground="Yellow" FontFamily="Impact"/>
                <TextBox Text="{Binding Enemyshottimechange, UpdateSourceTrigger=Explicit}" PreviewTextInput="TextBox_PreviewTextInput" MaxLength="2" HorizontalContentAlignment="Right" VerticalAlignment="Center" Width="70" Opacity="0.65"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" >
                <Label Content="Boss shooting intensity" Foreground="Yellow" FontFamily="Impact"/>
                <TextBox Text="{Binding Bossshottimechange, UpdateSourceTrigger=Explicit}" PreviewTextInput="TextBox_PreviewTextInput" MaxLength="2" HorizontalContentAlignment="Right" VerticalAlignment="Center" Width="70" Opacity="0.65"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" >
                <Label Content="Enemies on the field" Foreground="Yellow" FontFamily="Impact"/>
                <TextBox Text="{Binding EnemySpawnCount, UpdateSourceTrigger=Explicit}" PreviewTextInput="TextBox_PreviewTextInput" MaxLength="2" HorizontalContentAlignment="Right" VerticalAlignment="Center" Width="70" Opacity="0.65"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" >
                <Label Content="Boss health" Foreground="Yellow" FontFamily="Impact"/>
                <TextBox Text="{Binding BossHealth, UpdateSourceTrigger=Explicit}" PreviewTextInput="TextBox_PreviewTextInput" MaxLength="3" HorizontalContentAlignment="Right" VerticalAlignment="Center" Width="70" Opacity="0.65"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" >
                <Label Content="Godmode ON/OFF" Foreground="Yellow" FontFamily="Impact"/>
                <CheckBox IsChecked="{Binding Godmode, UpdateSourceTrigger=Explicit}" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.65"/>
            </StackPanel>
        </StackPanel>
        <Button Content="Save" Click="Save_Button_Click" FontFamily="Agency FB" Margin="150,264,0,52"/>
        <Button Content="Close" Click="Back_Button_Click"  FontFamily="Agency FB" Margin="0,261,150,52"/>
    </Grid>
</Window>
